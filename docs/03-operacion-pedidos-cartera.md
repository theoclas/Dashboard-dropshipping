# Fase 3 - Operacion Pedidos y Cartera

## Entregado inicial
- Endpoint de pedidos multiempresa:
  - `GET /api/orders`
  - `POST /api/orders` (upsert por `companyId + externalOrderId`)

## Siguientes incrementos recomendados
- CRUD completo de cartera y productos detalle.
- Notas manuales por pedido.
- Estados unificados con mapeo por transportadora.
